# Conda specification for the custom training environment built in Lab 4.
name: diabetes-train-env
channels:
  - conda-forge
dependencies:
  - python=3.10
  - pip
  - pip:
      - scikit-learn~=1.5.0
      - pandas~=2.2.0
      - mlflow~=2.19.0
      - azureml-mlflow
      - mltable
